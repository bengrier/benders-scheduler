.DS_Store
~$*.xlsx
__pycache__/
*.pyc
.claude/
node_modules/
.dev.vars
.wrangler/

# Personal access links are credentials: each one can mark that player's row,
# and the captain link can reset the whole season. Never commit them.
# Root-anchored so scripts/make_links.mjs stays tracked.
/*[Ll]ink*
*[Ll]inks*.txt
*[Ll]inks*.rtf
